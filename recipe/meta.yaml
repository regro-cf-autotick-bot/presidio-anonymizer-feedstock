{% set name = "presidio-anonymizer" %}
{% set version = "2.2.364" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/microsoft/presidio/archive/refs/tags/{{ version }}.tar.gz
  sha256: 9e8475df63612a46bbb12d1881177bc6042e9a130fe29317bbc852d9f028bc4e

build:
  noarch: python
  script: cd {{ name }} && {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - poetry-core
  run:
    - python >={{ python_min }},<4
    - cryptography <44.1
  run_constrained:
    - flask >=1.1
    - gunicorn

test:
  imports:
    - presidio_anonymizer
  requires:
    - python {{ python_min }}
    - pip
  commands:
    - pip list
    - pip check

about:
  home: https://github.com/microsoft/presidio
  summary: 'Presidio Anonymizer package - replaces analyzed text with desired values.'
  description: |
    Presidio Anonymizer package - replaces analyzed text with desired values.
  license: MIT
  license_file: LICENSE
  doc_url: https://microsoft.github.io/presidio
  dev_url: https://github.com/microsoft/presidio

extra:
  recipe-maintainers:
    - phemps
    - gulyasi
    - julcsii
    - geoHeil
    - heckert
